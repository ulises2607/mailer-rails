<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <style>
      body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
      .container { max-width: 600px; margin: 0 auto; padding: 20px; }
      .header { background-color: #f8f9fa; padding: 15px; border-radius: 5px; margin-bottom: 20px; }
      .content { background-color: #ffffff; padding: 20px; border: 1px solid #ddd; border-radius: 5px; }
      .field { margin-bottom: 10px; }
      .label { font-weight: bold; color: #555; }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h2>🎓 <%= @subject %></h2>
        <p>Se ha recibido una nueva inscripción</p>
      </div>
      
      <div class="content">
        <div class="field">
          <span class="label">Nombre completo:</span> <%= @data['fullname'] || @data['nombre'] || 'No especificado' %>
        </div>
        
        <div class="field">
          <span class="label">Fecha de nacimiento:</span> <%= @data['birthDate'] || @data['fechaNacimiento'] || 'No especificado' %>
        </div>
        
        <div class="field">
          <span class="label">DNI:</span> <%= @data['dni'] || 'No especificado' %>
        </div>
        
        <div class="field">
          <span class="label">Domicilio:</span> <%= @data['address'] || @data['domicilio'] || 'No especificado' %>
        </div>
        
        <div class="field">
          <span class="label">Teléfono:</span> <%= @data['phone'] || @data['telefono'] || 'No especificado' %>
        </div>
        
        <div class="field">
          <span class="label">Email:</span> <%= @data['email'] || 'No especificado' %>
        </div>
        
        <div class="field">
          <span class="label">Profesión:</span> <%= @data['profession'] || @data['profesion'] || 'No especificado' %>
        </div>
        
        <div class="field">
          <span class="label">Estudiante:</span> <%= @data['student'] || @data['estudiante'] || 'No especificado' %>
        </div>
        
        <div class="field">
          <span class="label">Tipo de asistente:</span> <%= @data['attendeeType'] || @data['tipoAsistente'] || 'No especificado' %>
        </div>
      </div>
      
      <div style="margin-top: 20px; padding: 10px; background-color: #e9ecef; border-radius: 5px; font-size: 12px; color: #6c757d;">
        <p>Correo enviado automáticamente desde el sistema de inscripciones del Instituto San Cayetano.</p>
        <p>Fecha: <%= Date.current.strftime("%d/%m/%Y") %></p>
      </div>
    </div>
  </body>
</html>
